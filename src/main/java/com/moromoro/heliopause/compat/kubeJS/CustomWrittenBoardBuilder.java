/*
package com.moromoro.heliopause.compat.kubeJS;

import com.moromoro.heliopause.block.WrittenBoardBlock;
import com.moromoro.heliopause.registry.BlockRegistry;
import dev.latvian.mods.kubejs.block.BlockBuilder;
import dev.latvian.mods.kubejs.block.BlockItemBuilder;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.world.level.block.Block;
import net.minecraft.world.level.block.state.BlockBehaviour;

public class CustomWrittenBoardBuilder extends BlockBuilder {
    public CustomWrittenBoardBuilder(ResourceLocation id) {
        super(id);
        this.itemBuilder = new BlockItemBuilder(id);
    }
    
    @Override
    public Block createObject() {
        return new WrittenBoardBlock(BlockBehaviour.Properties.copy(BlockRegistry.BLACKBOARD.get()));
    }
}
*/
